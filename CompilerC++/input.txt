int main_var = 0;
int x = 123;
int y = 0x1F;
int z = 007;

bool flag = true;
void func() {
    a = b + c * 2 - (d >> 1);
    if (a == b) a = a + 1;
    if (c != 0) c = c << 2;
    x = x >= 10;
}

/* комментарий в несколько строк
   должен быть проигнорирован
*/

switch (x) {
  case 0:
    break;
  default:
    break;
}

// комментарий в одну строку

// создаем ошибки лексеру
//bad = !a; // одиночный символ '!' в начале выражения
//hex_err = 0x; // 0x без шестнадцатеричного числа